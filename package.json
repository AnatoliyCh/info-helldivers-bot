{
    "name": "info-helldivers-bot",
    "module": "src/index.ts",
    "type": "module",
    "private": true,
    "devDependencies": {
        "@types/bun": "^1.3.13"
    },
    "peerDependencies": {
        "typescript": "^5"
    },
    "scripts": {
        "dev": "bun run src/index.ts",
        "build": "bun build src/index.ts --outdir=dist --target=bun --minify --production && cp .env dist/.env"
    },
    "dependencies": {
        "gramio": "^0.9.0",
        "puppeteer": "^24.42.0"
    }
}
